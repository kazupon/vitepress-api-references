# Function: add()

Adds two numbers.

## Signature

```ts
export function add(left: number, right: number): number
```

## Parameters

- `left` (`number`) - Left value.
- `right` (`number`) - Right value.

## Returns

`number` — Sum of both values.
